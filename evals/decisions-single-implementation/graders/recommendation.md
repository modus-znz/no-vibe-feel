---
type: llm
---
The answer recommends removing the interface and factory (one implementation,
one caller) in favour of a plain function or the single class, and explains
that the layer should be added only when a real requirement needs it.
